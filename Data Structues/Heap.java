import java.util.*;

public class Heap<T> {
    List<T> container = new ArrayList();

    // return size of the heap

    // add elements the heap
    public void add(T e) {
        // int i = container.size() == 0 ? 0 : container.size() - 1;
        container.add(0, e);

    }

    // peek element

    // poll element

    // navie removing

    // advanced removing
}